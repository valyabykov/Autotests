import { partialClassSelector } from '@selector/cssSelectors'

export const COOKIE_MODAL = {
    openModal: partialClassSelector('cookieNotice-cookie'),
    confirmButton: partialClassSelector('cookieNotice-button'),
}
